<li>
	{{burger_name}}

	<button class="change-devour" data-id="{{id}}" data-newdevour="{{devoured}}">
		{{#if devoured}}Eat it up!{{else}}Get one!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">Get rid of it!</button>
</li>
